import { LogoFn } from '../types/logo';

const crystalCubism: LogoFn = ({ color, description }) => `
A logo for ${description},
${color} colored,
simple, vector, Crystal Cubism.
`;

export default crystalCubism;
