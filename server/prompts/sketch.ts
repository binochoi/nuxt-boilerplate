import { LogoFn } from '../types/logo';

const sketch: LogoFn = ({ color, description }) => `
A simple logo of ${description},
${color} colored,
sketch, flat, no realistic photo details
`;

export default sketch;
